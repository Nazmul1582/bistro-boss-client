import Head from "../Shared/Head/Head";

const Menu = () => {
    return (
        <section>
            <Head pageTitle="Menu" />
            <p>Menu Page</p>
        </section>
    );
};

export default Menu;